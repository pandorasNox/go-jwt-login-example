package main

func main () {}

type user struct {
	name string
	password string
}

func login(user user) {}

func isAutherized(resourceName "")

// client makes first call to server and gets new identity & identityToken (registration)
// getInfo(identityToken) return number of request with this token
